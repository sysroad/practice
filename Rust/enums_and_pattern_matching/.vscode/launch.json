{
    "version": "0.2.0",
    "configurations": [
        {
            "type":"lldb",
            "request":"launch",
            "name": "Debug(lldb)",
            "program": "${workspaceRoot}/target/debug/enums_and_pattern_matching",
            "cwd":"${workspaceRoot}",
            "preLaunchTask": "cargo",
            "sourceLanguages": ["rust"]
        }
    ],
    "compounds": []
}
